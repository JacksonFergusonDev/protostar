| Option | Description |
| :--- | :--- |
| *(No args)* | Displays a guide with setup instructions for all supported shells. |
| `<shell>` | Target shell (`bash`, `zsh`, `fish`, `powershell`). Emits the raw completion script to stdout. |
| `--json` | Emits a structured JSON payload containing the shell script or list of supported shells. |
