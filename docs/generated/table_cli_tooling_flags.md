| Enable Flag | Disable Flag | Description |
| :--- | :--- | :--- |
| `--direnv` | `--no-direnv` | Activate the project's environment whenever you enter its folder |
| `--markdownlint` | `--no-markdownlint` | Check Markdown files for formatting mistakes, with relaxed rules |
| `--rumdl` | `--no-rumdl` | Check and format Markdown files quickly |
| `--ruff` | `--no-ruff` | Find common bugs and style problems in Python code, and format it |
| `--mypy` | `--no-mypy` | Check type hints to catch mistakes before the code runs |
| `--ty` | `--no-ty` | Check type hints quickly, with Astral's type checker |
| `--pyrefly` | `--no-pyrefly` | Check type hints quickly, with Meta's type checker |
| `--pytest` | `--no-pytest` | Run the project's tests |
| `--pre-commit` | `--no-pre-commit` | Run the project's checks automatically each time you commit |
| `--prek` | `--no-prek` | Run the project's checks automatically each time you commit, faster |
| `--commitizen` | `--no-commitizen` | Write commit messages in a standard form that sets the next version |
| `--renovate` | `--no-renovate` | Open pull requests that keep dependencies up to date |
| `--codecov` | `--no-codecov` | Report how much of the code the tests exercise, on each pull request |
| `--zensical` | `--no-zensical` | Build a documentation website from Markdown files |
| `--readthedocs` | `--no-readthedocs` | Publish the documentation website on Read the Docs |
| `--ci` | `--no-ci` | Run the checks and tests on GitHub for every push and pull request |
| `--release` | `--no-release` | Publish the package to PyPI when you push a version tag |
| `--just` | `--no-just` | Give the project's common commands short names, like `just test` |
| `--agents` | `--no-agents` | Tell coding assistants how to work on the project |
| `--community` | `--no-community` | Add the files GitHub shows people who want to contribute |
| `--docker` | `--no-docker` | Multi-stage `Dockerfile` and `.dockerignore` container scaffolding |
