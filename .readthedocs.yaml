version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.12"
  jobs:
    post_install:
      - curl -LsSf https://astral.sh/uv/install.sh | sh
      - $HOME/.local/bin/uv sync --group docs

mkdocs:
  configuration: mkdocs.yml
